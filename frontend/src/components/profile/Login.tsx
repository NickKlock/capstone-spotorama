import {Box} from "@mui/material";
import {UserLoginRequest} from "../../models/User";

type LoginProps = {
    handleLoginRequest(userLoginRequest: UserLoginRequest): void
}
export default function Login(props: LoginProps) {


    return (<Box>

    </Box>)
}